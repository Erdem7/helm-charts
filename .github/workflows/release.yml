name: release
on:
  push:
    branches:
      - main
env:
  GITHUB_TOKEN: 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
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish Helm charts
        uses: stefanprodan/helm-gh-pages@master
        with:
          token: $GITHUB_TOKEN
