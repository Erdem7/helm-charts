name: release
on:
  push:
    branches:
      - main
env:
  GITHUB_TOKEN: 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
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish Helm charts
        uses: stefanprodan/helm-gh-pages@master
        with:
          token: $GITHUB_TOKEN
